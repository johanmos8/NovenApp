# <Feature / change name>

**Status:** Draft
**Owner approval:** _pending — do not implement until this line names who approved and when_
**Scope:** <files/screens/modules this touches>
**Related:** <links to other specs, docs/, or CLAUDE.md principles this builds on>

---

## 1. Intent

What problem this solves and why it matters, in a short paragraph. Tie it back to
what the app actually is (a 9-day seasonal devotional) where relevant — see
`CLAUDE.md` §3.

## 2. Non-goals

What this spec explicitly does **not** cover, so scope doesn't creep during
implementation. If it's tempting to also fix/refactor something adjacent, name it
here and route it to a separate spec instead (see `CLAUDE.md` §1.2).

## 3. Proposed behavior / UX

What the user sees and does. States, flows, edge cases (e.g. what happens before the
window opens, after it closes, offline, etc). Tables/diagrams welcome.

## 4. Decisions

The concrete design/technical decisions, and the reasoning behind each — not just
*what* was chosen but *why*, and what the alternative(s) were. Where more than one
reasonable option exists, lay them out with a recommendation; **this section is what
the owner is actually approving.**

## 5. Open questions

Anything that needs an answer from the owner before this spec can move to `Approved`.
Empty this section out (resolve or explicitly defer each item) before requesting
approval.

## 6. Tasks

_Leave empty until Status is `Approved`._ Ordered checklist of implementation steps
once approved.

## 7. Follow-ups

Known deferred work this spec intentionally does not do — candidates for future specs.

## 8. Files

| File | Change |
|---|---|
| | |
